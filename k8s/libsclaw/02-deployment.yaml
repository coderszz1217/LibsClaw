apiVersion: apps/v1
kind: Deployment
metadata:
  name: libsclaw-standalone
  namespace: libsclaw-standalone-ns
  labels:
    app: libsclaw-standalone
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: libsclaw-standalone
  template:
    metadata:
      labels:
        app: libsclaw-standalone
    spec:
      containers:
        - name: libsclaw
          image: libsclaw:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: "Asia/Shanghai"
          ports:
            - containerPort: 6185
              name: webui
            - containerPort: 6199
              name: qq-ws
            # - containerPort: 6195
            #   name: wecom-wh
            # - containerPort: 6196
            #   name: qq-off-wh
          volumeMounts:
            - name: data
              mountPath: /LibsClaw/data
            - name: localtime
              mountPath: /etc/localtime
              readOnly: true
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: libsclaw-data-pvc
        - name: localtime
          hostPath:
            path: /etc/localtime
            type: File